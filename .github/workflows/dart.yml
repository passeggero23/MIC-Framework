name: Flutter Build

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.0'
          channel: 'stable'

      - name: Surgical Repair & SDK Upgrade
        run: |
          # 1. Creazione progetto temporaneo
          flutter create repair_temp --project-name mic_app --platforms android
          cp -r repair_temp/android .
          rm -rf repair_temp
          
          # 2. Upgrade SDK version da 19 a 21 (richiesto dalla camera)
          sed -i 's/minSdkVersion .*/minSdkVersion 21/g' android/app/build.gradle

      - name: Inject Permissions
        run: |
          sed -i '/<application/i \    <uses-permission android:name="android.permission.CAMERA" />' android/app/src/main/AndroidManifest.xml

      - name: Build APK
        run: |
          flutter pub get
          flutter build apk --release

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-release
          path: build/app/outputs/flutter-apk/app-release.apk
