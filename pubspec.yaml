name: mic_framework
description: "Framework modulare per suite decisionali."
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

  flutter:
  uses-material-design: true

  # Abilitiamo la cartella per i modelli AI e le configurazioni
  assets:
    - assets/models/
