name: mic_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  # Queste sono le chiavi che mancano per togliere le X rosse:
  camera: ^0.10.5+5
  tflite_flutter: ^0.10.0
  path_provider: ^2.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  # Qui diciamo a Flutter di guardare dentro la tua cartella assets
  assets:
    - assets/model.tflite
